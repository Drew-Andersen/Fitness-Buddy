// Workout variables
const completedWorkouts = document.getElementById('completed-workouts');
const upcomintWorkouts = document.getElementById('upcoming-workouts');

// Render the workout section
function renderWorkouts () {
    // Created elements to hold the variables
    
}

// Progress bar
const start = document.getElementById('starting-weight');
const startSumbit = document.getElementById('start-weight-btn');
const current = document.getElementById('current-weight');
const currentSumbit = document.getElementById('current-weight-btn');
const goal = document.getElementById('goal-weight');
const goalSumbit = document.getElementById('goal-weight-btn');
const bar = document.getElementById('progress-bar');

