<div class="profile-container">
    <h1 class="font-bold text-white text-center text-lg">{{name}}'s Dashboard</h1>
    <section id="goals-section" class="text-center mb-5">
        <div id="goals" class="grid grid-cols-3 my-6">
            <div id="starting-weight" class="col-span-1">
                <label class="text-white font-bold">Starting Weight: </label>
                <input type="number" id="input-starting-weight" class="rounded-md text-center" placeholder="230" />
                <button type="submit" id="start-weight-btn" class="bg-green-600 hover:bg-green-800 text-white font-bold py-2 px-4 rounded m-0.5">Submit</button>
                <div id="starting-div"></div>
            </div>
            <div id="current-weight" class="col-span-1">
                <label class="text-white font-bold">Current Weight: </label>
                <input type="number" id="input-current-weight" class="rounded-md text-center" placeholder="225" />
                <button type="submit" id="current-weight-btn" class="bg-green-600 hover:bg-green-800 text-white font-bold py-2 px-4 rounded m-0.5">Submit</button>
                <div id="current-div"></div>
            </div>
            <div id="goal-weight" class="col-span-1">
                <label class="text-white font-bold">Goal Weight: </label>
                <input type="number" id="input-goal-weight" class="rounded-md text-center" placeholder="200" />
                <button type="submit" id="goal-weight-btn" class="bg-green-600 hover:bg-green-800 text-white font-bold py-2 px-4 rounded m-0.5">Submit</button>
                <div id="goal-div"></div>
            </div>
        </div>
        <div id="progress" class="text-white font-bold">83%
            <div class="w-full h-6 bg-gray-200 rounded-full text-white">
                <div id="progress-bar" class="h-6 bg-green-600 rounded-full dark:bg-green-700 border border-white" style="width:83%;"{{!--{percentage}--}}></div>
            </div>
        </div>
    </section>
    {{#if workout.length}}
    <section id="workouts-section" class="grid grid-cols-2">
        {{!--#if date_creatd < today --}}
        <div id="completed-workouts" class="col-span-1">
            <h3 class="text-white text-center font-bold">Completed Workouts</h3>
            {{#each workout}}
            <div class="justify-center">
                <div class="card border bg-gray-400 p-5 rounded-md">
                    <a href="/workout/{{id}}">
                        <div class="card-body">
                            <h2 class="card-title">{{format_date date_created}}</h2>
                            <p>Exercises</p> {{!-- Can delete --}}
                        </div>
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
        {{!-- {{else}} --}}
        <div id="upcoming-workouts" class="col-span-1">
            <h3 class="text-white text-center font-bold">Upcoming Workouts</h3>
            {{#each workout}}
            <div class="justify-center">
                <div class="card border bg-gray-400 p-5 rounded-md">
                    <a href="/workout/{{id}}">
                        <div class="card-body">
                            <h2 class="card-title">{{date_created}}</h2>
                            <p>Exercises</p> {{!-- Can delete --}}
                        </div>
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
        {{!-- {{/if}} --}}
    </section>
    {{else}}
    <div class="border border-white rounded-lg bg-gray-500 m-5">
        <p class="text-center text-lg p-3 font-semibold">No workouts recorded</p>
    </div>
    {{/if}}
</div>

<script src="/js/dashboard.js"></script>